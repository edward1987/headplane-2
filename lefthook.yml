pre-commit:
  commands:
    lint:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json,jsonc}"
      run: corepack pnpm lint --fix {staged_files}
      stage_fixed: true
    format:
      glob: "*"
      run: corepack pnpm format --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    check_go:
      glob: "*.go"
      run: gofmt -w {staged_files}
      stage_fixed: true
